class Aircraft{
    id: string;
    takeOff: number;
    landing: number;

    constructor(id: string, takeOff: number, landing: number){
        this.id = id;
        this.takeOff = takeOff;
        this.landing = landing;
    }

}